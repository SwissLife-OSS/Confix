# Confix.CodeGeneration

Build-only activation catalog generator for Confix. Reference with `PrivateAssets="all"` in the application. Unconditional top-level registrations are generated automatically; use a shared Confix module for other setup.

See the code-first guide in the Confix repository for configuration and examples. Requires .NET 10.
