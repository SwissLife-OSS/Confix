# Confix.Abstractions

Minimal attributes for declaring Confix configuration sections and required keys. No package dependencies.

See the code-first guide in the Confix repository for configuration and examples. Requires .NET 10.
